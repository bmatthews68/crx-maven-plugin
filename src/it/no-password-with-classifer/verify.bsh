import java.io.*;

File file = new File( basedir, "target/HelloWorld-1.0.0-SNAPSHOT-debug.crx" );
if ( !file.isFile() )
{
    throw new FileNotFoundException( "Could not find generated CRX: " + file );
}